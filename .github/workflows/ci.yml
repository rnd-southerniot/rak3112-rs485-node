name: CI

on:
  push:
    branches: [main, "phase/**", "feat/**", "fix/**"]
  pull_request:
    branches: [main]

jobs:
  pre-commit:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Install LLVM 18 (clang-format) + pre-commit
        run: |
          brew install llvm@18
          pipx install pre-commit
          echo "/opt/homebrew/opt/llvm@18/bin" >> "$GITHUB_PATH"
      - name: Run pre-commit
        run: pre-commit run --all-files

  idf-build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Build with ESP-IDF v5.5.4 (pristine)
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.5.4
          target: esp32s3
          path: firmware

  gitleaks:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gitleaks v8.30.1
        run: |
          curl -fsSL -o /tmp/gitleaks.tgz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          sudo tar -xzf /tmp/gitleaks.tgz -C /usr/local/bin gitleaks
      - name: Scan
        run: gitleaks detect --no-banner --redact

  host-tests:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Verify scaffolding present
        run: |
          test -d tests/host
          test -f tests/host/CMakeLists.txt
          # Real ctest activation lands in Phase 4.

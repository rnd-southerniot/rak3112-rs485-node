# Pi 5 RS-485 scanner/profiling station — runtime deps (Python 3.11+).
# Kept minimal + arm64-wheel-friendly for the Pi. Pin majors; the station is not a library.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.35
pyserial>=3.5,<4.0
httpx>=0.27,<0.29
# dev/test
pytest>=8.0,<9.0

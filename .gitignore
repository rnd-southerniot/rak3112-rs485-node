# ESP-IDF / PlatformIO build outputs
build/
.pio/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
*.bin
*.elf
*.map

# macOS clutter
.DS_Store
.AppleDouble
.LSOverride

# IDE
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo

# Secrets — never commit
.env
.env.local
*.key
*.pem
secrets/

# Build / test outputs
build/host/
test_build/

# Python tooling
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Logs
*.log

# Per-board LoRaWAN provisioning secrets (DevEUI/AppKey) — never commit
firmware/.env
firmware/.env.*

# Generated LoRaWAN credentials header (contains AppKey) — never commit
firmware/main/lora_credentials.h

# clangd / editor index cache
.cache/

# session/hook artifacts (local only)
.claude/data/
logs/
firmware/logs/
.venv-api/

# Schematic snapshots

| Version | Date imported | Source | Notes |
|---|---|---|---|
| v1.1 | 2026-05-01 | `~/Developer/projects/pcb-design/rs485-node/` (EasyEDA Pro) | Initial snapshot. Filenames `RAK3112 + RS485 P2P Node V1.1 Schematic.pdf` and `RAK3112 + RS485 P2P Node V1.1.epro` carry the corrected `RAK3112` part number (earlier `ProPrj_RAK3312+…` filename had a typo, since fixed). Note: the "P2P Node" string in the title is flagged as **OQ-8** in the parent CLAUDE.md — verify intent (LoRa P2P vs LoRaWAN) before Phase 5 stack selection. |

These files are reference snapshots only — never edit here. Canonical edits happen in `rnd-southerniot/rak3112-rs485-node-hw` (when created).

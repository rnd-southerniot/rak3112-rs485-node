# Reference (read-only provenance)

Read-only snapshot vendored from the team's reference repo. **Do not edit these files** — they are
the proven source from which `../profiles/*.json` were derived and against which they are validated.
Canonical source of truth for any change is the upstream repo; re-snapshot on a new tag.

| Field | Value |
|---|---|
| Source repo | `RedwanRifat07/modbus-simulated-dataset` (reference only) |
| Commit | `ac6cd3fb02b8423f0bac5b746867d8f038b620bf` |
| Snapshot date | 2026-06-24 |

## Contents
- `arduino-code/` — proven Arduino readers (wire-level authority: registers, FC, word order, scale).
- `datasheets/` — manufacturer datasheets (ranges/units/names cross-check).
- `firmware/components/` — the team's generated ESP-IDF readers (`meter_*.c`) + simulators +
  payload encoders (`payload_*.c`) + shared headers. Authoritative register maps + scales.
- `tools/` — the team's ChirpStack decoders.

These were generated by the team using the "Add a new field sensor" prompt. They are excluded from
this repo's pre-commit hooks (see `.pre-commit-config.yaml`) so they stay byte-identical to upstream.
